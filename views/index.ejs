<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log In</title>
    <meta name="csrf-token" content="<%= csrfToken %>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Montserrat', sans-serif;
        }

        body {
            background: linear-gradient(270deg, #df8908 10%, #b415ff 100%);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .heading {
            font-size: 3.5em;
            color: #ffffff;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.4);
        }

        .container {
            display: flex;
            width: 80%;
            max-width: 1200px;
            background-color: aliceblue;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .image-container {
            flex: 1;
            background: url('https://www.otacfitness.com/wp-content/uploads/2018/08/Sports-and-Athletes-Website-Header.png') no-repeat center center;
            background-size: cover;
        }

        .form-container {
            flex: 1;
            display: grid;
            grid-template-rows: auto 1fr auto;
            justify-content: center;
            align-items: center;
            padding: 30px;
        }

        h2 {
            font-size: 2em;
            color: #161414;
            text-align: center;
            margin-bottom: 20px;
        }

        .input-box {
            position: relative;
            width: 100%;
            margin: 15px 0;
            border-bottom: 1px solid #110f0f;
        }

        .input-box label {
            position: absolute;
            top: 50%;
            left: 5px;
            transform: translateY(-50%);
            font-size: 1em;
            color: #100f0f;
            pointer-events: none;
            transition: 0.5s;
        }

        .input-box input {
            width: 100%;
            height: 50px;
            background: transparent;
            border: none;
            outline: none;
            font-size: 1em;
            color: #0e0d0d;
            padding: 0 5px;
        }

        .input-box input:focus ~ label,
        .input-box input:valid ~ label {
            top: -5px;
        }

        button {
            width: 100%;
            height: 45px;
            background-color: #bcca28;
            border: none;
            border-radius: 40px;
            cursor: pointer;
            font-size: 1em;
            color: #f3eded;
            font-weight: 500;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #3b3ea7;
        }

        .register-link {
            font-size: 0.9em;
            color: #0b0a0a;
            text-align: center;
            margin-top: 20px;
        }

        .register-link p a {
            color: #110e0e;
            text-decoration: none;
            font-weight: 600;
        }

        .register-link p a:hover {
            text-decoration: underline;
        }

        .alert {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <h1 class="heading">Welcome To Sports-Scheduler</h1>
    <div class="container">
        <div class="image-container"></div>
        <div class="form-container">
            <% if (errorMessage.length > 0) { %>
                <div class="alert alert-danger" role="alert">
                    <%= errorMessage %>
                </div>
            <% } %>

            <h2>Log In</h2>

            <form action="/signinsubmit" method="POST">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                <div class="input-box">
                    <input id="email" name="email" type="email" required>
                    <label for="email">Email</label>
                </div>

                <div class="input-box">
                    <input id="password" name="password" type="password" required>
                    <label for="password">Password</label>
                </div>

                <button type="submit">Sign In</button>

                <div class="register-link">
                    <p>Don't have an account? <a href="/signup">Sign up</a></p>
                </div>
            </form>
        </div>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Event</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(120deg, #84fab0, #8fd3f4);
        }
        .container {
            background: #fff;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            width: 100%;
            max-width: 500px;
        }
        h2 {
            text-align: center;
            font-size: 2rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 20px;
        }
        .btn-back {
            background-color: #6c63ff;
            color: #fff;
            border: none;
            margin-bottom: 20px;
            transition: background-color 0.3s;
        }
        .btn-back:hover {
            background-color: #5548c8;
        }
        .form-label {
            font-weight: 600;
            color: #555;
        }
        .form-control {
            border: 2px solid #e0e0e0;
            border-radius: 5px;
            padding: 10px;
        }
        .form-control:focus {
            border-color: #6c63ff;
            box-shadow: 0 0 5px rgba(108, 99, 255, 0.5);
        }
        textarea.form-control {
            resize: none;
        }
        .input-group {
            background: linear-gradient(120deg, #e0f7fa, #b2ebf2);
            border-radius: 8px;
            padding: 10px;
            margin-bottom: 15px;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
        }
        .input-group:hover {
            background: linear-gradient(120deg, #b2ebf2, #84fab0);
        }
        .btn-primary {
            background-color: #6c63ff;
            border: none;
            padding: 10px 20px;
            font-size: 1rem;
            font-weight: bold;
            width: 100%;
            transition: background-color 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #5548c8;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Create Event</h2>
        <button class="btn btn-back" onclick="window.location.href='/admindashboard'">← Back to Dashboard</button>
        <form action="/create-event" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="input-group">
                <label for="title" class="form-label">Title</label>
                <input type="text" class="form-control" id="title" name="title" placeholder="Enter event title" required>
            </div>
            <div class="input-group">
                <label for="date" class="form-label">Date</label>
                <input type="date" class="form-control" id="date" name="date" required>
            </div>
            <div class="input-group">
                <label for="time" class="form-label">Time</label>
                <input type="time" class="form-control" id="time" name="time" required>
            </div>
            <div class="input-group">
                <label for="venue" class="form-label">Venue</label>
                <input type="text" class="form-control" id="venue" name="venue" placeholder="Enter venue details" required>
            </div>
            <div class="input-group">
                <label for="team_limit" class="form-label">Team Limit</label>
                <input type="number" class="form-control" id="team_limit" name="team_limit" placeholder="Enter maximum team limit" required>
            </div>
            <div class="input-group">
                <label for="description" class="form-label">Description</label>
                <textarea class="form-control" id="description" name="description" rows="4" placeholder="Enter event description" required></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Create Event</button>
        </form>
    </div>
</body>
</html>
